version: '3.8'

services:
    redis:
        image: redis:latest
        container_name: redis-proj
        ports:
         - "${REDIS_PORT}:6379"
        command: redis-server
        volumes:
         - redis_data:/data

volumes:
    redis_data:

